#!/usr/bin/env sh

# Run formatting on staged files, then lint, tests, and build. Abort commit if any step fails.
pnpm exec lint-staged && pnpm lint && pnpm test -- --run --reporter=dot && pnpm build 